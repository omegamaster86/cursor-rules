---
title: HTTP Client
impact: MEDIUM
impactDescription: HTTP 通信
tags: http, dio, flutter
---

## HTTP Client

**既定:** Supabase `functions.invoke`（追加の Dio クライアントは不要）。

`dio` が pubspec にあっても lib 未使用なら問題にしない。外部 REST が必要なときのオプション。
