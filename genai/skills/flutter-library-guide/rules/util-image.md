---
title: Image Utilities
impact: LOW
impactDescription: 画像パッケージ
tags: image, flutter
---

## Image Utilities

`cached_network_image` は **未導入・任意**。`flutter_svg` / `image_picker` が pubspec にあっても lib 未使用なら必須扱いにしない。
