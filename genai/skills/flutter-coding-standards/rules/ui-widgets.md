---
title: UI Widgets
impact: MEDIUM
impactDescription: 共通ウィジェット
tags: ui, widgets, flutter
---

## UI Widgets

`core/widgets/` は **フラット**（`logout_button.dart`, `loading_screen.dart`, `main_shell.dart`）。

`buttons/primary_button.dart` のような深いサブツリーは規模に応じて導入（必須ではない）。
